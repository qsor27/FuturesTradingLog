# Spec Summary (Lite)

Fix execution storage to store individual executions instead of pre-paired trades, enabling the position builder to correctly calculate positions using FIFO methodology with accurate average prices and P&L per account.
