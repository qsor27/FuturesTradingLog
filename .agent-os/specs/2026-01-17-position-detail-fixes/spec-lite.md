# Spec Summary (Lite)

Fix the position detail page to show per-execution P&L results (matching entry/exit pairs with FIFO), resolve candle data loading issues preventing chart display, and add entry/exit arrow markers on the price chart for trade visualization.
